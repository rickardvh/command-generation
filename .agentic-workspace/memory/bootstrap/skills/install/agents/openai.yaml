description: Finish bootstrap installation review from the target repo's temporary bootstrap workspace.
prompt:
  default_prompt: "Use $install from the repo's `.agentic-workspace/memory/bootstrap/skills/` path to finish bootstrap installation conservatively."
